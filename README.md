# 전처리기
**전처리기란?** 
- PreCompiler
- 컴파일러가 시작하기 전에 먼저 처리해주는 것

헤더파일에 사용하는 전처리기
- 헤더의 중복을 방지
- a.c와 b.c 가 c.h를 사용할 때 infdef가 정의되어 있지 않다면 내용이 중복 컴파일 됨

ex) #ifndef A	// A가 정의되어 있지 않다면
	#define A	// A를 정의
	- 내용
	#endif		// A끝
